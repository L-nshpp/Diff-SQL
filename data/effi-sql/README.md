# Effi-SQL Benchmark Placeholder

Download the Diff-SQL HuggingFace dataset:

```text
https://huggingface.co/datasets/Lnsshp/Diff-SQL
```

Place the benchmark file here:

```text
benchmark.jsonl
```

Expected HuggingFace path:

```text
data/effi-sql/benchmark.jsonl
```

The repository also includes a copy under `benchmarks/eff-sql-pg.jsonl` for the default evaluation command.
