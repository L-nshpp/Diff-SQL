project: diff_sql

paths:
  eval_input_dir: benchmarks
  eval_output_dir: outputs/postgres
  benchmark_file: benchmarks/eff-sql-pg.jsonl
  postgresql_scale_dumps: postgre_scale_table_dumps
  tpch_raw_data_3g: tpch/raw_data/3g
  patch_generator_train: data/patch-generator-training-dataset/train.parquet
  constraint_aligner_sft_warmup_train: data/constraint-aligner-training-dataset/train.parquet
  grpo_train: null
  hf_benchmark_file: data/effi-sql/benchmark.jsonl

training:
  sft_config: configs/model_training/sft.yaml
  grpo_config: configs/model_training/grpo.yaml
  output_root: checkpoints

evaluation:
  dialect: postgres
  benchmark_scale: scale
  sql_mode: patch
  response_field: prediction
